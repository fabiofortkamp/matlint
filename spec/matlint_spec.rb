# frozen_string_literal: true

RSpec.describe Matlint do
  it "has a version number" do
    expect(Matlint::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
